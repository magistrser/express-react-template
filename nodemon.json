{
  "ignore": ["**/*.test.ts", "**/*.spec.ts", "node_modules", "src/client"],
  "watch": ["src/server"],
  "exec": "ts-node --project tsconfig.server.json -r tsconfig-paths/register src/server/index.ts",
  "ext": "ts",
  "env": {
    "CLIENT_PATH": "dist_client"
  }
}
